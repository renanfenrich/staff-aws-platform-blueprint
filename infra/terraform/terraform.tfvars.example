cost_center = "portfolio"
owner       = "platform-team"

# Cost-bearing modules remain disabled by default.
deployment_enabled = false

# Required only after a reviewed image workflow publishes an immutable artifact.
# container_image = "ACCOUNT.dkr.ecr.REGION.amazonaws.com/REPOSITORY@sha256:..."
